* Systemd files for my containers

Link this systemd directory to ~/.config/containers/systemd.

#+begin_src shell

# must use full path
ln -s $(COMPLETE PATH TO PWD)/systemd ~/.config/containers/systemd

#+end_src
